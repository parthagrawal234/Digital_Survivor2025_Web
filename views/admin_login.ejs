<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="/stylesheets/output.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&display=swap" rel="stylesheet">
    <style> .font-orbitron { font-family: 'Orbitron', sans-serif; } </style>
</head>
<body class="bg-slate-900 flex items-center justify-center min-h-screen font-orbitron">
    <div class="w-full max-w-sm p-8 space-y-8 bg-slate-800/50 border border-green-500 rounded-2xl shadow-2xl shadow-green-500/10">
        <div>
            <h2 class="text-3xl font-black text-center text-white">Admin Access</h2>
            <p class="mt-2 text-center text-sm text-green-400">Secure Authentication Required</p>
        </div>
        <form class="mt-8 space-y-6" action="/admin/login" method="POST">
            <div class="rounded-md shadow-sm -space-y-px">
                <div>
                    <input id="username" name="username" type="text" required class="appearance-none rounded-none relative block w-full px-3 py-3 border border-green-500 bg-slate-900 text-green-300 placeholder-green-400/50 focus:outline-none focus:ring-green-500 focus:border-green-500 focus:z-10 sm:text-sm rounded-t-md" placeholder="Username">
                </div>
                <div>
                    <input id="password" name="password" type="password" required class="appearance-none rounded-none relative block w-full px-3 py-3 border border-green-500 bg-slate-900 text-green-300 placeholder-green-400/50 focus:outline-none focus:ring-green-500 focus:border-green-500 focus:z-10 sm:text-sm rounded-b-md" placeholder="Password">
                </div>
            </div>
            <% if (error) { %>
                <p class="text-center text-sm text-red-400"><%= error %></p>
            <% } %>
            <div>
                <button type="submit" class="group relative w-full flex justify-center py-3 px-4 border border-transparent text-sm font-medium rounded-md text-slate-900 bg-green-500 hover:bg-green-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500 focus:ring-offset-slate-900 transition-colors duration-300">
                    Authenticate
                </button>
            </div>
        </form>
    </div>
</body>
</html>

