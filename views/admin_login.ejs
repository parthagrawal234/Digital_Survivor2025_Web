<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="stylesheets/output.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&display=swap" rel="stylesheet">
    <style> .font-orbitron { font-family: 'Orbitron', sans-serif; } </style>
</head>
<body class="bg-slate-900 flex items-center justify-center min-h-screen font-orbitron">
    <div class="w-full max-w-md p-8 space-y-8 bg-slate-800/50 border border-indigo-500 rounded-2xl shadow-2xl shadow-indigo-500/10">
        <div>
            <h2 class="text-4xl font-black text-center text-white">Admin Access</h2>
            <p class="mt-2 text-center text-sm text-indigo-400">Secure Authentication Required</p>
        </div>
        <form class="mt-8 space-y-6" action="/admin/login" method="POST">
            <div class="rounded-md shadow-sm -space-y-px">
                <div>
                    <input id="username" name="username" type="text" required class="...[tailwind classes]..." placeholder="Admin Username">
                </div>
                <div>
                    <input id="password" name="password" type="password" required class="...[tailwind classes]..." placeholder="Password">
                </div>
            </div>

            <% if (error) { %>
                <p class="text-center text-red-400"><%= error %></p>
            <% } %>

            <div>
                <button type="submit" class="...[tailwind classes]...">
                    Authenticate
                </button>
            </div>
        </form>
    </div>
</body>
</html>
