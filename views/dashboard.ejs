<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="/stylesheets/output.css" rel="stylesheet">
    <script src="/socket.io/socket.io.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Inter:wght@400;500&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Inter', sans-serif; }
        .font-orbitron { font-family: 'Orbitron', sans-serif; }
    </style>
</head>
<body class="bg-slate-900 text-gray-300">
    <div class="fixed inset-0 z-[-1] bg-gradient-to-br from-gray-900 via-[#100f24] to-indigo-900"></div>

    <!-- Header -->
    <header class="bg-slate-900/50 backdrop-blur-sm border-b border-indigo-500/30">
        <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-16">
                <div>
                    <h1 class="font-orbitron text-xl font-bold text-white">Team: <span class="text-indigo-400"><%= user.teamId %></span></h1>
                    <p class="text-xs text-gray-400">Delegate: <span class="font-medium text-gray-300"><%= user.delegateId %></span></p>
                </div>
                <form action="/api/logout" method="POST">
                    <button type="submit" class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-lg transition-colors duration-300 text-sm">Logout</button>
                </form>
            </div>
        </nav>
    </header>

    <!-- Main Content -->
    <main class="relative z-10 flex flex-col items-center justify-center text-center p-4 mt-20 md:mt-32">
        <div class="max-w-3xl">
            <h2 class="font-orbitron text-4xl md:text-6xl font-black text-white leading-tight">Welcome, <%= user.role %></h2>
            <p class="max-w-2xl mx-auto mt-6 text-lg text-gray-300">
                You have successfully authenticated. The system is ready for your command. Your mission for Round 2 is now available.
            </p>
            <div class="mt-10">
                <button id="startBtn" class="inline-block bg-indigo-600 hover:bg-indigo-700 text-white font-bold text-lg px-8 py-4 rounded-lg shadow-lg shadow-indigo-600/20 transform hover:scale-105 transition-all duration-300">
                    Start Mission
                </button>
            </div>
        </div>
    </main>

<script>
    const teamId = '<%= user.teamId %>';
    const delegateId = '<%= user.delegateId %>';
    const userRole = '<%= user.role %>';
    const socket = io();

    // In case the 3rd player is on this page when the mission starts,
    // this listener will redirect them correctly.
    socket.on('start-mission', () => {
        let roleUrl = '/';
        if (userRole === 'cyber') roleUrl = '/role/cyber';
        if (userRole === 'eng') roleUrl = '/role/eng';
        if (userRole === 'opera') roleUrl = '/role/opera';
        window.location.href = roleUrl;
    });

    document.getElementById('startBtn').addEventListener('click', () => {
        // 1. Tell the server this player is ready.
        socket.emit('player-ready', { teamId, delegateId });
        
        // 2. Immediately navigate to the waiting room.
        window.location.href = '/waiting';
    });
</script>
</body>
</html>

