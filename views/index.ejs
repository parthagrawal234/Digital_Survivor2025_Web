<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="/stylesheets/output.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Inter:wght@400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            /* FIX: Apply the background directly to the body for full coverage */
            background-image: url('/images/index_background.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed; /* This creates a simple parallax effect and ensures it covers the scroll area */
        }
        .font-orbitron {
            font-family: 'Orbitron', sans-serif;
        }
    </style>
</head>
<body class="bg-slate-900 text-white antialiased">

    <!-- Main Content Wrapper -->
    <div class="relative z-10 flex flex-col min-h-screen bg-black/50">

        <!-- HEADER: Rebuilt to be mobile-responsive -->
        <header class="w-full px-4 md:px-8 py-6">
            <div class="max-w-7xl mx-auto flex flex-col md:flex-row justify-between items-center gap-6 md:gap-0">
                
                <!-- On mobile, this will be empty to allow the logo to be centered. On desktop, it takes up space. -->
                <div class="hidden md:flex w-1/3"></div>
                
                <!-- Centered Logo and Title -->
                <div class="text-center">
                    <a href="/" class="inline-block">
                        <svg class="h-12 w-12 mx-auto text-cyan-400" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M16.5 10.5V6.75a4.5 4.5 0 00-9 0v3.75m-.75 11.25h10.5a2.25 2.25 0 002.25-2.25v-6.75a2.25 2.25 0 00-2.25-2.25H6.75a2.25 2.25 0 00-2.25 2.25v6.75a2.25 2.25 0 002.25 2.25z" />
                        </svg>
                    </a>
                    <h1 class="font-orbitron text-2xl font-bold text-white mt-2 tracking-widest">
                        Cyber Survivor
                    </h1>
                    <p class="text-sm text-cyan-400 font-medium tracking-wider">
                        Digital Dystopia
                    </p>
                </div>
                
                <!-- Right-aligned Navigation -->
                <nav class="w-full md:w-1/3 flex justify-center md:justify-end">
                    <ul class="flex items-center space-x-6">
                        <li><a href="/register" class="text-gray-300 hover:text-cyan-400 font-medium transition-colors duration-300">Register</a></li>
                        <li><a href="/login" class="text-gray-300 hover:text-cyan-400 font-medium transition-colors duration-300">Login</a></li>
                        <li>
                            <a href="<%= isLoggedIn ? '/dashboard' : '/login' %>" class="bg-cyan-500/10 border border-cyan-500 hover:bg-cyan-500/20 text-cyan-400 font-bold py-2 px-4 rounded-lg transition-colors duration-300">
                                Competition
                            </a>
                        </li>
                    </ul>
                </nav>
            </div>
        </header>

        <!-- Hero Content Section (Adjusted margin for mobile) -->
        <main class="flex-grow flex flex-col items-center justify-center text-center px-4">
            <h2 class="font-orbitron text-5xl md:text-7xl font-black text-white leading-tight drop-shadow-lg">
                Breach the System.
            </h2>
            <h3 class="font-orbitron text-5xl md:text-7xl font-black text-transparent bg-clip-text bg-gradient-to-r from-cyan-300 to-blue-400 leading-tight drop-shadow-lg mt-2">
                Claim Your Rank.
            </h3>
            <p class="max-w-2xl mx-auto mt-6 text-lg text-gray-300">
                An immersive cybersecurity arena where only the sharpest survive. Do you have what it takes to climb the leaderboard?
            </p>
            <div class="mt-10">
                <a href="<%= isLoggedIn ? '/dashboard' : '/login' %>" class="inline-block bg-cyan-500 hover:bg-cyan-600 text-gray-900 font-bold text-lg px-8 py-4 rounded-lg shadow-lg shadow-cyan-500/20 transform hover:scale-105 transition-all duration-300">
                    Enter the Arena
                </a>
            </div>
        </main>
    </div>
    
    <!-- Parallax script is no longer needed -->
</body>
</html>

