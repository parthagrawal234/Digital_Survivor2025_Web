<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="stylesheets/output.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Inter:wght@400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            overflow: hidden; /* Prevents scrollbars from the oversized background */
        }
        .font-orbitron {
            font-family: 'Orbitron', sans-serif;
        }
        #background-parallax-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background-color: #0c0a1c; 
        }
        #background-parallax {
            position: absolute;
            top: -5%; 
            left: -5%;
            width: 110%;
            height: 110%;
            background-image: url('/images/index_background.jpg');
            background-size: cover;
            background-position: center;
            transition: transform 0.2s ease-out;
        }
    </style>
</head>
<body class="bg-gray-900 text-white antialiased">

    <!-- Background Container -->
    <div id="background-parallax-container">
        <div id="background-parallax"></div>
    </div>

    <!-- Main Content Wrapper -->
    <div class="relative z-10 flex flex-col min-h-screen">

        <!-- Header Section -->
        <header class="w-full px-4 md:px-8 py-6">
            <div class="max-w-7xl mx-auto flex justify-between items-center">
                <!-- Left Spacer -->
                <div class="w-1/3"></div>
                
                <!-- Centered Logo and Title -->
                <div class="w-1/3 text-center">
                    <a href="/" class="inline-block">
                        <svg class="h-8 w-8 mx-auto text-cyan-400" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M16.5 10.5V6.75a4.5 4.5 0 00-9 0v3.75m-.75 11.25h10.5a2.25 2.25 0 002.25-2.25v-6.75a2.25 2.25 0 00-2.25-2.25H6.75a2.25 2.25 0 00-2.25 2.25v6.75a2.25 2.25 0 002.25 2.25z" />
                        </svg>
                    </a>
                    <h1 class="font-orbitron text-2xl font-bold text-white mt-2 tracking-widest">
                        Cyber Survivor
                    </h1>
                    <p class="text-sm text-cyan-400 font-medium tracking-wider">
                        Digital Dystopia
                    </p>
                </div>
                
                <!-- Right-aligned Navigation -->
                <nav class="w-1/3 flex justify-end">
                    <ul class="flex items-center space-x-6">
                        <li>
                            <a href="/register" class="text-gray-300 hover:text-cyan-400 font-medium transition-colors duration-300">Register</a>
                        </li>
                        <li>
                            <a href="/login" class="text-gray-300 hover:text-cyan-400 font-medium transition-colors duration-300">Login</a>
                        </li>
                        <li>
                            <!-- UPDATED: Competition button now has conditional logic -->
                            <% if (isLoggedIn) { %>
                                <a href="/dashboard" class="bg-cyan-500/10 border border-cyan-500 hover:bg-cyan-500/20 text-cyan-400 font-bold py-2 px-4 rounded-lg transition-colors duration-300">
                                    Competition
                                </a>
                            <% } else { %>
                                <a href="/login" class="bg-cyan-500/10 border border-cyan-500 hover:bg-cyan-500/20 text-cyan-400 font-bold py-2 px-4 rounded-lg transition-colors duration-300">
                                    Competition
                                </a>
                            <% } %>
                        </li>
                    </ul>
                </nav>
            </div>
        </header>

        <!-- Hero Content Section -->
        <main class="flex-grow flex flex-col items-center justify-center text-center px-4 -mt-20">
            <h2 class="font-orbitron text-5xl md:text-7xl font-black text-white leading-tight drop-shadow-lg">
                Enter the Grid.
            </h2>
            <h3 class="font-orbitron text-5xl md:text-7xl font-black text-transparent bg-clip-text bg-gradient-to-r from-cyan-300 to-blue-400 leading-tight drop-shadow-lg mt-2">
                Escape the Darkness.
            </h3>
            <p class="max-w-2xl mx-auto mt-6 text-lg text-gray-300">
                The Phantoms breach the first layer of Nexus Core — but the AI strikes back, creating a No-Comm Zone. Each Phantom must rely on their expertise as Cybersec Experts, Engineers, and Operation Speacialist tackle separate challenges. Only by combining their fragments of access can they reopen a secure channel and launch the next web exploitation attack.
            </p>
            <div class="mt-10">
                <% if (isLoggedIn) { %>
                    <a href="/dashboard" class="inline-block bg-cyan-500 hover:bg-cyan-600 text-gray-900 font-bold text-lg px-8 py-4 rounded-lg shadow-lg shadow-cyan-500/20 transform hover:scale-105 transition-all duration-300">
                        Enter the Arena
                    </a>
                <% } else { %>
                    <a href="/login" class="inline-block bg-cyan-500 hover:bg-cyan-600 text-gray-900 font-bold text-lg px-8 py-4 rounded-lg shadow-lg shadow-cyan-500/20 transform hover:scale-105 transition-all duration-300">
                        Enter the Arena
                    </a>
                <% } %>
            </div>
        </main>
    </div>

    <script>
        const bg = document.getElementById('background-parallax');
        
        window.addEventListener('mousemove', function(e) {
            const x = e.clientX;
            const y = e.clientY;
            const moveX = (x - window.innerWidth / 2) * -0.01; 
            const moveY = (y - window.innerHeight / 2) * -0.01;
            bg.style.transform = `translateX(${moveX}px) translateY(${moveY}px)`;
        });
    </script>
</body>
</html>